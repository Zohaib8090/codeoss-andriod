---
phase: 01-the-shell-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/src/main/cpp/CMakeLists.txt, app/src/main/cpp/native-lib.cpp, app/build.gradle.kts]
autonomous: true
requirements: [Foundation, PTY Support]
must_haves:
  truths:
    - "Native NDK build is configured and operational"
    - "JNI bridge can spawn a shell process via forkpty"
  artifacts:
    - path: "app/src/main/cpp/CMakeLists.txt"
      provides: "NDK build configuration"
    - path: "app/src/main/cpp/native-lib.cpp"
      provides: "PTY bridge implementation"
  key_links:
    - from: "app/build.gradle.kts"
      to: "app/src/main/cpp/CMakeLists.txt"
      via: "externalNativeBuild configuration"
---

<objective>
Implement the native PTY bridge using NDK and JNI.

Purpose: Provides the low-level capability to spawn and control a shell process on Android.
Output: Native C++ library and JNI interface.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure NDK Build</name>
  <files>app/src/main/cpp/CMakeLists.txt, app/build.gradle.kts</files>
  <read_first>app/build.gradle.kts</read_first>
  <action>Create app/src/main/cpp/CMakeLists.txt defining the native-lib. Update app/build.gradle.kts to include externalNativeBuild { cmake { path = file("src/main/cpp/CMakeLists.txt") } }.</action>
  <verify>ls app/src/main/cpp/CMakeLists.txt</verify>
  <acceptance_criteria>
    - CMakeLists.txt exists and defines native-lib
    - build.gradle.kts contains externalNativeBuild
  </acceptance_criteria>
  <done>NDK build is linked to the project</done>
</task>

<task type="auto">
  <name>Task 2: Implement PTY forkpty wrapper</name>
  <files>app/src/main/cpp/native-lib.cpp</files>
  <read_first>app/src/main/cpp/native-lib.cpp</read_first>
  <action>Implement JNI function to spawn a shell using forkpty (or openpty/fork/login_tty wrapper for Android). Handle master file descriptor return.</action>
  <verify>grep "Java_com_zohaib_codeossandriod_PtyBridge_createPty" app/src/main/cpp/native-lib.cpp</verify>
  <acceptance_criteria>
    - native-lib.cpp contains PTY spawning logic
    - JNI entry point for createPty is defined
  </acceptance_criteria>
  <done>Native PTY bridge is implemented</done>
</task>

</tasks>

<verification>
- [ ] ./gradlew assembleDebug succeeds
- [ ] JNI symbols are present in the built library
</verification>

<success_criteria>
- All tasks completed
- Native bridge is ready for Kotlin integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-the-shell-foundation/01-01-SUMMARY.md`
</output>
