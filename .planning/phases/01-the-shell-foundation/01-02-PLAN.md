---
phase: 01-the-shell-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [app/src/main/kotlin/com/zohaib/codeossandriod/PtyBridge.kt, app/src/main/kotlin/com/zohaib/codeossandriod/MainActivity.kt]
autonomous: true
requirements: [Integrated Terminal]
must_haves:
  truths:
    - "Kotlin layer can communicate with the native PTY master"
    - "Terminal output from the shell is displayed in the UI"
  artifacts:
    - path: "app/src/main/kotlin/com/zohaib/codeossandriod/PtyBridge.kt"
      provides: "Kotlin PTY bridge wrapper"
    - path: "app/src/main/kotlin/com/zohaib/codeossandriod/MainActivity.kt"
      provides: "Integrated Terminal UI"
  key_links:
    - from: "app/src/main/kotlin/com/zohaib/codeossandriod/PtyBridge.kt"
      to: "native-lib"
      via: "System.loadLibrary"
---

<objective>
Integrate the native PTY bridge with the Jetpack Compose UI.

Purpose: Completes the loop from the shell process to the user's screen.
Output: Functional terminal component in the app.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-the-shell-foundation/01-01-SUMMARY.md
@app/src/main/kotlin/com/zohaib/codeossandriod/MainActivity.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PtyBridge Kotlin Wrapper</name>
  <files>app/src/main/kotlin/com/zohaib/codeossandriod/PtyBridge.kt</files>
  <read_first>app/src/main/kotlin/com/zohaib/codeossandriod/MainActivity.kt</read_first>
  <action>Create PtyBridge.kt that loads the native library and exposes createPty, read, write, and resize functions. Use a thread or Coroutine to read from the master FD.</action>
  <verify>ls app/src/main/kotlin/com/zohaib/codeossandriod/PtyBridge.kt</verify>
  <acceptance_criteria>
    - PtyBridge.kt exists and contains external function declarations
    - Native library is loaded in a companion object
  </acceptance_criteria>
  <done>Kotlin bridge wrapper is ready</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Terminal UI</name>
  <files>app/src/main/kotlin/com/zohaib/codeossandriod/MainActivity.kt</files>
  <read_first>app/src/main/kotlin/com/zohaib/codeossandriod/MainActivity.kt</read_first>
  <action>Update TerminalComponent in MainActivity.kt to display output from PtyBridge. Implement a simple scrolling buffer for the text.</action>
  <verify>grep "PtyBridge" app/src/main/kotlin/com/zohaib/codeossandriod/MainActivity.kt</verify>
  <acceptance_criteria>
    - MainActivity.kt uses PtyBridge to start a shell
    - TerminalComponent renders the received byte stream
  </acceptance_criteria>
  <done>Terminal UI is functional</done>
</task>

</tasks>

<verification>
- [ ] App builds and runs on an emulator/device
- [ ] Terminal displays the shell prompt (e.g., "$")
</verification>

<success_criteria>
- All tasks completed
- User can interact with the shell through the app UI
</success_criteria>

<output>
After completion, create `.planning/phases/01-the-shell-foundation/01-02-SUMMARY.md`
</output>
